torch>=2.1.0
torchvision
diffusers>=0.30.0
transformers>=4.38.0
accelerate>=0.28.0
sentencepiece
protobuf
fastapi
uvicorn[standard]
pillow
requests
# DreamO imports cv2; use headless build in Docker/GPU images (no libGL.so.1).
opencv-python-headless>=4.8.0
# DreamO (try-on + twin): NOT installable via pip — upstream repo has no package
# metadata. Clone https://github.com/bytedance/DreamO and set:
#   export DREAMO_SRC=/path/to/DreamO
# (folder that contains dreamo/dreamo_pipeline.py). See gpu_service/dreamo_inference.py
